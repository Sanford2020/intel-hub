# TASK-20260521-M5-B: Archives / Trends API

## Task Info

- Type: 后端 / API
- Priority: P0
- Status: TODO
- Owner: Windsurf · Backend Skill
- Sprint: S1 · M5
- Prereq: TASK-20260521-M5-A DONE

## Goal

实现 `docs/plans/M5-daily-archive-trends.md` §6 三个端点 + 文档同步。

- `GET /api/v1/archives` 列表（分页 + from/to 过滤）
- `GET /api/v1/archives/{date}` 详情（briefing + metrics）
- `GET /api/v1/archives/trends` 7 种 metric + `tag` / `category`
- `GET /api/v1/archives/trends/category-heat`（§9 分类热度）

## Scope

### Allowed Changes

| Path | Purpose |
| --- | --- |
| `backend/app/modules/archives/router.py` | 路由 |
| `backend/app/modules/archives/schemas.py` | 增响应 schema |
| `backend/app/modules/archives/service.py` | 增 `list_archives` / `get_archive` / `trends_series` / `category_heat` |
| `backend/app/api/v1/api.py` | `include_router` 注册 |
| `docs/api.md` | 新增 /archives 章节 |
| `packages/shared-types/**` | TypeScript 类型（如已自动生成则刷新）|

### Forbidden

- 修改 M5-A 的 metrics 聚合逻辑（如发现 bug：开 BLOCKED，不要私自改）
- 修改 `articles` / `intelligence_reports` 模块
- 前端代码（C 卡）
- 测试（D 卡）

## Validation

```bash
# Backend up
uvicorn backend.app.main:app --reload

# 列表
curl 'http://localhost:8000/api/v1/archives?from=2026-05-01&to=2026-05-21'

# 详情
curl 'http://localhost:8000/api/v1/archives/2026-05-20'

# 趋势
curl 'http://localhost:8000/api/v1/archives/trends?days=30&metric=articles_created'
curl 'http://localhost:8000/api/v1/archives/trends?days=30&metric=tag_count&tag=ai'
curl 'http://localhost:8000/api/v1/archives/trends/category-heat?days=30'
```

所有响应必须：

- 200 + 合法 JSON
- 空数据返回 `points: []` 而非 500
- `from/to` 越界 → 400

## Risks

- 趋势 `tag_count` 是从 `metrics_json.tags_top` 还是 articles JOIN 计算？**默认从 `metrics_json` 读**，避免扫全表。
- 大区间（days>90）必须分页或拒绝；当前 v1 限 90 天，超出返回 400。

## Handoff

```
Changed: <files>
Verified: 4 curl 全 200
Risks: <any>
Next: M5-C（UI 串接）
Files: <touched>
```
