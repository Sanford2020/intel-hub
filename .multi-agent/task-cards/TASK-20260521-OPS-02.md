# TASK-20260521-OPS-02: validate_project.sh + GitHub Actions CI

## Task Info

- Type: DevOps / CI
- Priority: P1
- Status: TODO
- Owner: Codex · Deployment Skill
- Sprint: S2 · M3.5

## Goal

把现有 PowerShell `validate_project.ps1` 等价为跨平台 bash 脚本，并接入 GitHub Actions，实现 push → 自动 pytest + 前端 build + docker compose config。

## Scope

### Allowed Changes

| Path | Purpose |
| --- | --- |
| `scripts/validate_project.sh` | **新建** bash 等价；Windows 用户继续用 .ps1 |
| `.github/workflows/ci.yml` | **新建** CI workflow |
| `docs/deployment.md` | 增 "CI / Validate" 章节 |
| `Makefile` | 追加 `make ci` 目标（如易做）|

### Forbidden

- 改 `backend/`、`apps/`、`workers/` 业务代码（如发现配置缺漏：开 BLOCKED）
- 改 `validate_project.ps1`（Windows 用户在用，禁动）

## Validation

```bash
# 本地
bash scripts/validate_project.sh           # 必须 全绿（或返回有意义错误）

# CI
# 推到分支 → GH Actions 应跑：
#   1. pytest（backend/tests）
#   2. npm run type-check && npm run build（apps/web）
#   3. docker compose config（不真起容器）
#   4. validate_project.sh
```

CI 必须：

- 不依赖真实 OPENAI_API_KEY / FEISHU_WEBHOOK_URL（用 mock / dummy）
- PostgreSQL 用 services 容器（postgres:16-alpine）
- Redis 用 services 容器（redis:7-alpine）
- 命中失败 → PR 红叉

## Risks

- 现有测试可能依赖 Windows 路径 / 时区：跑前先 `echo $TZ=UTC` 与 `Asia/Shanghai` 各一次，发现差异立即记录到 REVIEW，**本卡只跑当前测试**，不修业务
- alembic 在 CI 需要新建库；先 `createdb intel_hub` 再 `alembic upgrade head`

## Handoff

```
Changed: scripts/validate_project.sh, .github/workflows/ci.yml, docs/deployment.md, Makefile
Verified: 本地 sh 绿；GH Actions 在本 PR 跑绿
Risks: <时区/路径差异>
Next: 老板验收 → 后续把"PR 必须绿"写入 CODEOWNERS
```
