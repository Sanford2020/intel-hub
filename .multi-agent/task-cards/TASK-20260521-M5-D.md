# TASK-20260521-M5-D: M5 Test + Acceptance + Review

## Task Info

- Type: 测试 / 验收 / Review
- Priority: P0
- Status: TODO
- Owner: Codex（Test + Documentation + Review）
- Sprint: S1 · M5
- Prereq: M5-A 起即可启动；C 完成后跑完整 e2e

## Goal

按 `docs/plans/M5-daily-archive-trends.md` Phase D：

1. `backend/tests/test_archives.py` 覆盖 service + API + metrics
2. 扩展 `scripts/acceptance-smoke.py` 检查 `/archives` / `/trends`
3. `run-daily-intel.ps1` 末尾增 `--archive` 选项
4. `docs/deployment.md` 增 Beat 06:15 archive 章节
5. `REVIEW.md` 写 Review 报告：性能 / JSON 大小 / 时区 / 幂等

## Scope

### Allowed Changes

| Path | Purpose |
| --- | --- |
| `backend/tests/test_archives.py` | 单元/集成测试 |
| `backend/tests/conftest.py` | 仅追加 archives fixture |
| `scripts/acceptance-smoke.py` | 增 archives check |
| `run-daily-intel.ps1` | 末尾 `--archive` flag |
| `docs/deployment.md` | Beat 06:15 + ARCHIVE_* env |
| `REVIEW.md` | 追加"M5 Archive Review"段 |

### Forbidden (Codex 边界铁律)

- `backend/app/**`（业务实现）
- `apps/web/src/app/**`（前端实现）
- `workers/tasks/archives/snapshot.py`（A 卡所有）
- 修改 M5-A/B/C 已交付的实现；如发现 bug：开新 task 或转 BLOCKED

## Validation

```bash
cd backend
PYTHONPATH=/workspace python -m pytest tests/test_archives.py -q       # 必须 全绿
PYTHONPATH=/workspace python -m pytest tests/ -q                       # 全量 不退化

python scripts/acceptance-smoke.py --check archives                     # 自定义子命令
```

Review 报告必填项：

- [ ] `briefing_json` size p95（warn 阈值 50KB）
- [ ] metrics SQL EXPLAIN 是否走索引
- [ ] 时区：跨日测试用例（23:50 / 00:10 各一条文章）
- [ ] 幂等：同日跑 2 次 row 数不变
- [ ] 失败处理：上游 briefing 抛错 → archive status = `partial`，不挂任务

## Handoff

```
Changed: <files>
Verified: pytest <N> passing / acceptance PASS / Review verdict <APPROVE|REQUEST_CHANGES>
Risks: <any>
Next: M5 全部 DONE → 通知 Master 关 Sprint S1 → 启动 S3 M6-ADR
Files: <touched>
```
