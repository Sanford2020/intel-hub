# TASK-20260521-M5-A: Daily Archive Data Layer + Celery Task

## Task Info

- Type: 数据 / 后端 / Worker
- Priority: P0
- Status: TODO
- Owner: Windsurf · Backend Skill
- Sprint: S1 · M5
- Prereq: TASK-20260521-M5-M Accepted

## Goal

按 `docs/plans/M5-daily-archive-trends.md` §4–5、§7（Phase A）实现：

1. `daily_archives` 表 + Alembic migration
2. `metrics.py` 单次 SQL 聚合
3. `ArchiveService.create_daily_archive()` UPSERT
4. Celery `archive_daily_snapshot` + Beat 06:15 UTC
5. `scripts/backfill-archives.py --days N`

## Scope

### Allowed Changes (Files owned)

| Path | Purpose |
| --- | --- |
| `backend/app/models/daily_archive.py` | SQLAlchemy 模型 |
| `backend/app/modules/archives/__init__.py` | 新模块 |
| `backend/app/modules/archives/metrics.py` | 聚合 SQL |
| `backend/app/modules/archives/service.py` | UPSERT + 读取 |
| `backend/app/modules/archives/schemas.py` | Pydantic（API 层 B 复用）|
| `backend/alembic/versions/*archives*.py` | 迁移 |
| `workers/tasks/archives/__init__.py` | 任务包导入（必须）|
| `workers/tasks/archives/snapshot.py` | Celery 任务 |
| `workers/celery_app.py` | Beat 06:15 注册 |
| `scripts/backfill-archives.py` | 历史补档脚本 |
| `backend/.env.example` | 增 `ARCHIVE_*` 配置项 |
| `.env.example` | 同上（保持一致）|

### Forbidden (Files avoided)

- `backend/app/modules/articles/**`、`backend/app/modules/intelligence/**`（只读不写）
- `apps/web/**`（前端 C 卡负责）
- `backend/app/modules/archives/router.py`（B 卡负责）
- 测试文件由 Codex D 卡写，不要自行写测试

## Validation

```bash
# 1. 迁移成功
cd backend && alembic upgrade head

# 2. 手动跑任务
celery -A workers.celery_app call workers.tasks.archives.snapshot.archive_daily_snapshot
psql -d intel_hub -c "SELECT archive_date, status, jsonb_object_keys(metrics_json) FROM daily_archives ORDER BY archive_date DESC LIMIT 3;"

# 3. 幂等
# 再跑一次 → 同一 archive_date 行只更新，不报错

# 4. backfill
python scripts/backfill-archives.py --days 7
```

## Risks

- `briefing_json` Top 20 截断逻辑：直接读 `build_daily_briefing_sync(limit=ARCHIVE_BRIEFING_LIMIT)`，不要重写简报。
- 时区：`archive_date` 使用 `ARCHIVE_TIMEZONE`（默认 Asia/Shanghai）下的 calendar date；写迁移时存 `DATE` 类型。
- 聚合 SQL：在 `(window_start, source_id)` / `(window_start, created_at)` 上使用现有索引；如缺失，先记 REVIEW，不要私加索引。

## Handoff (5 行)

```
Changed: <files>
Verified: alembic + manual task + idempotent
Risks: <any>
Next: M5-B（API 层）
Files: <touched>
```
