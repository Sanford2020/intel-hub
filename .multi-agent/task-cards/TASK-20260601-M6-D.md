# TASK-20260601-M6-D: M6 测试 + 部署 + 文档

## Task Info

- Type: 测试 / 部署 / 文档
- Priority: P0
- Status: TODO
- Owner: Codex（Test + Deployment + Documentation）
- Sprint: S3 · M6
- Prereq: M6-A 起即可启动；C 完成后跑 e2e

## Goal

1. 补全鉴权测试矩阵（login / refresh / logout / 403 / tenant 隔离 / refresh 黑名单）
2. 现有所有业务测试更新为带 auth header / cookie
3. 写部署文档：JWT_SECRET 生成、bootstrap admin、prod 必关 `AUTH_BRIEFING_PUBLIC`
4. `docker-compose.prod.yml` 增鉴权 env 注入 + 提示
5. `docs/api.md` 同步 OpenAPI 鉴权说明 + 每端点角色

## Scope

### Allowed Changes (Files owned)

| Path | Purpose |
| --- | --- |
| `backend/tests/test_auth.py` | login/refresh/logout/me/csrf |
| `backend/tests/test_rbac.py` | 矩阵 12 组合 |
| `backend/tests/test_tenant_isolation.py` | A 用户读不到 B 数据 |
| `backend/tests/conftest.py` | 增 `admin_client` / `analyst_client` / `viewer_client` fixtures |
| `backend/tests/test_*.py`（现有） | **仅** 更新为带 auth fixture；不改业务断言 |
| `docs/deployment.md` | §JWT 配置 / §首管理员 / §prod 检查清单 |
| `docs/api.md` | 每端点标注角色矩阵（与 M6-B 输出一致） |
| `docker-compose.prod.yml` | 注入 `JWT_SECRET` `AUTH_BRIEFING_PUBLIC=false` |
| `scripts/generate-jwt-secret.sh` | 输出 64 字符随机 |
| `scripts/validate_project.sh` | 增 "JWT_SECRET 非空且 ≥ 32 字符" 检查 |
| `.github/workflows/ci.yml` | 注入 dummy `JWT_SECRET` |
| `REVIEW.md` | 关闭"商业 Auth 未定义"风险条 |

### Forbidden

- `backend/app/**` 业务实现（M6-A/B 范围）
- `apps/web/src/**`（M6-C 范围）
- 改 RBAC 矩阵（已在 ADR-03 定义）

## Validation

```bash
cd backend
PYTHONPATH=/workspace python -m pytest tests/ -q
# 必须全绿；总数 ≥ 旧版 + 30（新增 auth/rbac/tenant）

# CI 也要绿
bash scripts/validate_project.sh

# 部署文档自查
grep -c 'JWT_SECRET' docs/deployment.md         # ≥ 3
grep -c 'AUTH_BRIEFING_PUBLIC' docs/deployment.md  # ≥ 2
```

Review 报告必填项（写入 `REVIEW.md`）：

- [ ] bcrypt 在 CI 慢吗？rounds=4 是否生效？
- [ ] refresh 黑名单：删 Redis key 后 refresh 应失败
- [ ] 公开端点白名单是否完整（grep `allow_anonymous`）
- [ ] 多租户 tenant_id 是否在所有业务 query 中
- [ ] 前端 401 死循环是否不可能触发

## Risks

- 现有测试 31+ 个全要改：用 conftest fixture 兜底，原断言不动
- alembic 迁移在 CI 缺 `default` tenant 数据：fixture 启动时 INSERT
- prod 部署忘改 `JWT_SECRET`：validate 脚本强校验

## Handoff

```
Changed: <files>
Verified: pytest <N> passing / CI green / Review APPROVE
Risks: <any>
Next: M6 全部 DONE → 通知 Master 收 Sprint S3 → 问老板选 M7 候选
Files: <touched>
```
