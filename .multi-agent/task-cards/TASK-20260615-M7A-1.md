# TASK-20260615-M7A-1: 双语简报 — 后端翻译管线

## Task Info

- Type: 后端 / AI 管线
- Priority: P1（M7-A 赛道；激活条件：老板选定 M7=A）
- Status: TODO
- Owner: Windsurf · Backend Skill
- Sprint: S4 · M7-A
- Prereq: ADR-20260521-04 Accepted + S3 M6 DONE

## Goal

按 ADR-04 实现 `intelligence_reports` 中文摘要落库：迁移加列 → Celery 链式翻译 → 服务层暴露 `summary_zh / title_zh` → API 返回。

## Scope

### Allowed Changes

| Path | Purpose |
| --- | --- |
| `backend/alembic/versions/*bilingual*.py` | 加 `summary_zh TEXT NULL` + `title_zh VARCHAR(512) NULL` + `translated_at TIMESTAMPTZ` |
| `services/ai/translate.py` | `translate_to_zh(text, model)` + mock fallback `[ZH-MOCK]` |
| `workers/tasks/translate/__init__.py` + `translate.py` | `translate_report` 任务，链在 `analyze_article` 之后 |
| `workers/celery_app.py` | 任务注册（不改 Beat） |
| `backend/app/modules/intelligence/schemas.py` | 响应增 `summary_zh` `title_zh` `lang_available: list[str]` |
| `backend/app/modules/intelligence/service.py` | 查询返回新字段 |
| `backend/app/modules/archives/metrics.py` | `briefing_json` 写入 `summary_zh`（如已译） |
| `backend/.env.example` | `BILINGUAL_ENABLED=true` `BILINGUAL_TARGET_LANG=zh` `BILINGUAL_MODEL=gpt-4o-mini` |

### Forbidden

- `apps/web/**`（A-2 卡）
- 测试（A-3 卡）
- 改简报 markdown 渲染逻辑

## Validation

```bash
cd backend && alembic upgrade head
celery -A workers.celery_app call workers.tasks.translate.translate_report --kwargs '{"report_id": 1}'
psql -c "SELECT id, title_zh, length(summary_zh) FROM intelligence_reports WHERE id=1;"
# → 非空（live 模式）或 [ZH-MOCK] 前缀（mock 模式）

curl http://localhost:8000/api/v1/articles/1/report
# → 200 JSON 含 summary_zh / title_zh / lang_available
```

## Risks

- OpenAI 配额：与 M7-C 联动；翻译走独立 `translate` 队列，可被 M7-C 限额降级
- 老报告未翻译：API 返回 `summary_zh=null` + `lang_available=['en']`，前端兜底

## Handoff

```
Changed: <files>
Verified: 迁移 + 任务 + API 返回新字段
Risks: <any>
Next: M7-A-2（前端切换）
Files: <touched>
```
