# TASK-20260521-M5-C: /archives + /trends 前端

## Task Info

- Type: 前端 / Dashboard
- Priority: P0
- Status: TODO
- Owner: Windsurf · Frontend Skill
- Sprint: S1 · M5
- Prereq: TASK-20260521-M5-B DONE（API schema 冻结）

## Goal

实现 `docs/plans/M5-daily-archive-trends.md` §7 + Phase C 前端：

- `/archives` 列表 + 详情抽屉（复用现有 briefing 卡片样式）
- `/trends` 折线/柱状图（先 CSS/SVG，避免引入重型 chart 库）
- Sidebar 加入两个入口

## Scope

### Allowed Changes

| Path | Purpose |
| --- | --- |
| `apps/web/src/app/archives/page.tsx` | 列表页 |
| `apps/web/src/app/archives/[date]/page.tsx`（或抽屉组件）| 详情 |
| `apps/web/src/app/trends/page.tsx` | 趋势页 |
| `apps/web/src/components/charts/**`（新建）| 极简 SVG 折线 / 柱图 |
| `apps/web/src/lib/intel-api.ts` | **仅追加** `getArchives` / `getArchive` / `getTrends` / `getCategoryHeat` |
| `apps/web/src/components/Sidebar*.tsx` | 导航入口（保持现有项） |

### Forbidden

- 任何 `backend/`、`workers/`、`services/` 文件
- 改 `intel-api.ts` 已有方法的签名
- 引入 chart.js / echarts / recharts 等重型库（如必须，需 ADR）

## Validation

```bash
cd apps/web
npm run type-check       # 必须 0 error
npm run build            # 必须 success
npm run dev              # 浏览器：
                         # /archives 看到日期列表
                         # 点击进入详情 → 看到 briefing markdown
                         # /trends 看到 30 天折线
```

UX 验收：

- 空状态：归档 0 条 → "暂无归档，等待今晚 06:15 Beat 自动生成"
- 错误：API 5xx → 顶部 toast 红条 + 重试按钮
- 移动端 < 768px 折线图不溢出

## Risks

- `briefing_json.markdown` 大块文本：用现有 markdown 渲染组件（参考 `/briefing` 页），勿重写
- 分类热度（category-heat）多分类对比：v1 用堆叠柱图或多色折线，**不要用饼图**

## Handoff

```
Changed: <files>
Verified: type-check + build + 3 路由手测
Risks: <any>
Next: 通知 Codex 跑 M5-D 全量验收
Files: <touched>
```
