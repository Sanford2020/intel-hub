# TASK-20260615-M7A-2: 双语简报 — 前端语言切换

## Task Info

- Type: 前端
- Priority: P1
- Status: TODO
- Owner: Windsurf · Frontend Skill
- Sprint: S4 · M7-A
- Prereq: TASK-20260615-M7A-1 DONE

## Goal

`/briefing`、`/articles/[id]`、`/archives/{date}` 三处增"中/英"切换，默认中文；缺中文译文时回退英文并提示。AI 翻译徽章必出。

## Scope

### Allowed Changes

| Path | Purpose |
| --- | --- |
| `apps/web/src/components/LangToggle.tsx` | 共享切换组件 + URL `?lang=zh/en` 同步 |
| `apps/web/src/lib/intel-api.ts` | 仅追加：返回类型增 `summary_zh` `title_zh` |
| `apps/web/src/app/briefing/page.tsx` | 接入 LangToggle |
| `apps/web/src/app/articles/[id]/page.tsx` | 同上 |
| `apps/web/src/app/archives/[date]/page.tsx` | 同上 |
| `apps/web/src/components/AITranslatedBadge.tsx` | 内联徽章"AI 翻译" + tooltip"看原文" |

### Forbidden

- `backend/`、`workers/`、`services/`
- 改 `LangToggle` 的全局 i18n（v1 仅资讯内容双语，UI 文案不本地化）

## Validation

```bash
npm run type-check && npm run build
npm run dev
# /briefing 默认中文；切换 EN 显示原文
# 没有 summary_zh 的旧报告：自动回退 EN + 顶部小条"AI 翻译尚未生成"
```

## Risks

- 切换闪烁：在 LangToggle 用 `useTransition`
- 简报 markdown 中嵌入英文段落：v1 不做段级混排，整体切换

## Handoff

```
Changed: <files>
Verified: type-check + build + 3 路由切换 OK
Risks: <any>
Next: M7-A-3（测试 + 文档）
```
